
# PART 2
result = 154 / 78

IO.puts "Result of 154 divided by 78: #{result}"
roundedInt = Kernel.round(result)
IO.puts "Nearest integer: #{roundedInt}"
integerN = Kernel.floor(result)
IO.puts "Integer part: #{integerN}"
